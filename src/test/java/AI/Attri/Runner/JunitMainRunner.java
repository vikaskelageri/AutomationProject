package AI.Attri.Runner;

/*import org.junit.runner.JUnitCore;
import org.junit.runner.Result;
import org.junit.runner.notification.Failure;

public class JunitMainRunner {
	public static void main(String[] args) {
		Result result = JUnitCore.runClasses(PreRequisitsRunner.class);
		for (Failure failure : result.getFailures()) {
			System.out.println(failure.toString());
		}
		System.out.println(result.wasSuccessful());

		Result failResult = JUnitCore.runClasses(TestRunner.class);
		for (Failure failure : failResult.getFailures()) {
			System.out.println(failure.toString());
		}
		System.out.println(failResult.wasSuccessful());
	}
}*/
